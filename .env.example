# Project-Specific Environment Variables for Twickenham Events
# Copy this file to .env and fill in your actual values
#
# NOTE: This project uses HIERARCHICAL environment loading:
# 1. Parent workspace .env (/home/ron/projects/.env) - SHARED settings
# 2. This project .env - PROJECT-SPECIFIC overrides
# 3. System environment variables - HIGHEST priority
#
# Most MQTT and API settings should go in the parent .env file!

# =============================================================================
# PROJECT-SPECIFIC OVERRIDES (if needed)
# =============================================================================

# MQTT Client ID (must be unique per project)
MQTT_CLIENT_ID=twickenham_events_client

# Project-specific Google Gemini settings (if different from shared)
# GEMINI_API_KEY=project_specific_api_key_here

# Project-specific MQTT overrides (if different from shared broker)
# MQTT_BROKER_URL=different-broker.example.com
# MQTT_USERNAME=different_username
# MQTT_PASSWORD=different_password

# =============================================================================
# SHARED SETTINGS REFERENCE (configured in parent .env)
# =============================================================================
# The following variables should be set in /home/ron/projects/.env:
#
# MQTT_BROKER_URL=your-broker.example.com
# MQTT_USERNAME=your_mqtt_username  
# MQTT_PASSWORD=your_mqtt_password
# MQTT_BROKER_PORT=8883
# MQTT_USE_TLS=true
# GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Create /home/ron/projects/.env with shared settings (MQTT, APIs)
# 2. Create this project's .env file for project-specific overrides
# 3. Run the application - it will load both files automatically
