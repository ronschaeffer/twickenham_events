# Project-Specific Environment Variables for Twickenham Events
# Copy


# Enable/disable MQTT integration (true/false)
MQTT_ENABLED=MQTT_ENABLED
# MQTT broker hostname or IP
MQTT_BROKER_URL=MQTT_BROKER_URL
# MQTT broker port (usually 1883 or 8883 for TLS)
MQTT_BROKER_PORT=MQTT_BROKER_PORT
# MQTT username for authentication
MQTT_USERNAME=MQTT_USERNAME
# MQTT password for authentication
MQTT_PASSWORD=MQTT_PASSWORD
# MQTT security mode (e.g., username, none)
MQTT_SECURITY=MQTT_SECURITY
# Main MQTT client ID for this project (used by service)
MQTT_CLIENT_ID=MQTT_CLIENT_ID
# Twickenham Events-specific client ID; Randomly set by code if not defined
MQTT_CLIENT_ID_TWICKENHAM=MQTT_CLIENT_ID_TWICKENHAM
# Enable TLS for MQTT connection (true/false)
MQTT_USE_TLS=MQTT_USE_TLS
# Enable/disable TLS certificate verification (true/false)
TLS_VERIFY=TLS_VERIFY


# Path to CA certificate for TLS (optional)
TLS_CA_CERT=TLS_CA_CERT
# Path to client certificate for TLS (optional)
TLS_CLIENT_CERT=TLS_CLIENT_CERT
# Path to client key for TLS (optional)
TLS_CLIENT_KEY=TLS_CLIENT_KEY


# Enable Home Assistant integration (true/false)
HOME_ASSISTANT_ENABLED=HOME_ASSISTANT_ENABLED


# Enable ICS calendar export (true/false)
CALENDAR_ENABLED=CALENDAR_ENABLED


# Google Gemini API key for AI features
GEMINI_API_KEY=GEMINI_API_KEY


# Web Server Configuration
# Enable/disable the web server (true/false)
WEB_SERVER_ENABLED=WEB_SERVER_ENABLED

# Web server host binding (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
WEB_SERVER_HOST=WEB_SERVER_HOST

# Web server port (8080 default, 47476 recommended for Docker deployments)
WEB_SERVER_PORT=WEB_SERVER_PORT

# Web Server External URL for Docker deployments (recommended)
# Set this to your complete external URL when running in Docker
# This serves as both the external URL override AND provides host IP detection
#
# Benefits of using this single setting:
# - Works for both URL generation and Docker host detection
# - Supports custom ports, protocols, and reverse proxy scenarios
# - Takes precedence over auto-detection methods
#
# Alternative Docker networking approaches:
# 1. Use --add-host=host.docker.internal:host-gateway flag (Linux/Unraid)
# 2. Use --network host for host networking
# 3. Let auto-detection find the host IP automatically
#
# Examples:
# WEB_SERVER_EXTERNAL_URL=http://10.10.10.20:47476       # Direct host IP
# WEB_SERVER_EXTERNAL_URL=https://twickenham.example.com # Behind reverse proxy
# WEB_SERVER_EXTERNAL_URL=http://unraid.local:47476      # Unraid hostname
WEB_SERVER_EXTERNAL_URL=WEB_SERVER_EXTERNAL_URL

# Enable web server access logging (true/false) - useful for debugging
WEB_SERVER_ACCESS_LOG=WEB_SERVER_ACCESS_LOG

# CORS (Cross-Origin Resource Sharing) settings for browser access
# Enable CORS support (true/false)
WEB_SERVER_CORS_ENABLED=WEB_SERVER_CORS_ENABLED

# CORS allowed origins (comma-separated list, or * for all origins)
# Examples: http://localhost:3000,https://mydomain.com or *
WEB_SERVER_CORS_ORIGINS=WEB_SERVER_CORS_ORIGINS

# Legacy: Docker Host IP (deprecated - use WEB_SERVER_EXTERNAL_URL instead)
# This is still supported for backward compatibility
# DOCKER_HOST_IP=DOCKER_HOST_IP


# Enable AI type detection (true/false)
TYPE_DETECTION_ENABLED=TYPE_DETECTION_ENABLED
# Enable AI event name shortening (true/false)
SHORTENING_ENABLED=SHORTENING_ENABLED
# Enable flags for AI shortening (true/false)
FLAGS_ENABLED=FLAGS_ENABLED
